<footer>
	<div class="container vp-xs-top-3 vp-xs-bottom-4">
		<div class="row">
			<div class="col-xs-8 offset-xs-2 vm-xs-bottom-3 text-xs-center">
				<p>{{ options.church_address }}<strong>10:00am at Blackman High School</strong><br />3956 Blaze Drive Murfreesboro, TN 37128</p>
			</div>
		</div>
		<div class="row">
			<div class="col-sm-4 col-lg-2 offset-lg-3 vm-xs-bottom-2 vm-md-bottom-0 text-xs-center text-sm-left">
				{% if footerNav1.get_items %}
					<h5>{{footerNav1.title}}</h5>
					<ul>
						{% for item in footerNav1.get_items %}
							<li class="{{item.classes | join(' ')}}"><a href="{{item.get_link}}">{{item.title}}</a></li>
						{% endfor %}
					</ul>
				{% endif %}
			</div>
			<div class="col-sm-4 col-lg-2 vm-xs-bottom-2 vm-md-bottom-0 text-xs-center text-sm-left">
				{% if footerNav2.get_items %}
					<h5>{{footerNav2.title}}</h5>
					<ul>
						{% for item in footerNav2.get_items %}
							<li class="{{item.classes | join(' ')}}"><a href="{{item.get_link}}">{{item.title}}</a></li>
						{% endfor %}
					</ul>
				{% endif %}
			</div>
			<div class="col-sm-4 col-lg-2 vm-xs-bottom-2 vm-md-bottom-0 text-xs-center text-sm-left">
				{% if footerNav3.get_items %}
					<h5>{{footerNav3.title}}</h5>
					<ul>
						{% for item in footerNav3.get_items %}
							<li class="{{item.classes | join(' ')}}"><a href="{{item.get_link}}">{{item.title}}</a></li>
						{% endfor %}
					</ul>
				{% endif %}
			</div>
		</div>
	</div>
	<div class="base">
		<div class="container">
			<div class="row">
				<div class="col-xs-12 text-xs-center">
					<p>2633 Merchants Walk, Murfreesboro, TN 37128 | 615.893.7444<br />
					<span>&copy; {{"now"|date('Y')}} River Oaks Community Church. All Rights Reserved.</span></p>
				</div>
			</div>
		</div>
	</div>
</footer>
